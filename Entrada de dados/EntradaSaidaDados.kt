// Escreva um programa que solicite ao usuário a entrada de dois números inteiros. O programa deve ler esses dois números, calcular a soma deles e, em seguida, exibir o resultado da soma na tela com uma mensagem descritiva (ex: "A soma é: X").

import java.util.Scanner;

fun main() {

    //Declaração das variáveis
    var num1;
    var num2;
 
    // Solicitação e recebimento de dados do usuário
    print("Digite o primeiro número: ")
    var scanner = Scanner(System.`in`);
    num1 = scanner.nextInt();

    print("Digite o segundo número: ")
    var scanner = Scanner(System.`in`);
    num2 = scanner.nextInt();




}
